using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NotCursed : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Wards of the evils
    }

    // Update is called once per frame
    void Update()
    {
        //Purifies Alex's evil soul
    }
}
